# 이어받기 — 대화가 날아갔을 때 여기부터

> 아빠가 다른 계정으로 로그인했다 돌아와서 대화 기록이 없을 수 있습니다.
> **새 에이전트는 이 문서 → `00-READ-FIRST.md` → 해당 `spec-*.md` 순서로 읽으세요.**
> 최종 수정: 2026-09-04

## 이 프로젝트가 뭔가
`D:\하준\HAJUN\game.html` — 아빠가 **6살 아들 하준이**를 위해 만든 한글학습 포켓몬 게임.
한 파일에 HTML+CSS+JS 전부 들어 있음(약 8,400줄).

- **하준이는 폰으로만 합니다.** 갤럭시 S25+ (384×832). 엄마 아이폰(375×812)으로도 가끔
- 배포: GitHub Pages `scotty123-wq/HAJUN`, `main` 브랜치. **백업과 웹사이트가 같은 push**
- `node build.js` 가 `sprites/*.webp` 331장을 base64 로 `game.html` 에 심고 `index.html`(약 8MB)을 만듦

## 일하는 방식 (아빠가 정한 것)
> "내가 지시한걸 너는 감독만 하고 모든작업은 서브 ai한테 시켜줘 어차피 단계별로자나? 너는 마지막 검증만"

- **메인 에이전트는 지시·검증·배포만.** 실제 `game.html` 편집은 서브에이전트에게
- **한 차수 끝날 때마다 자동으로 build + commit + push.** 아빠가 따로 요청 안 해도 됨
- 아빠가 오래 답이 없으면 그대로 진행 (자율권 부여받음)
- `game.html` 은 한 파일이라 **동시 편집 불가.** 편집 에이전트는 항상 1개만

## 지금까지 배포된 것 (18차까지)
| 차수 | 내용 |
|---|---|
| 1~5차 | 공식 일러스트 331장 / 폰 안전 / 턴제 배틀 / 퀴즈 15종 / 기술 42종·이펙트 / 진화 선택 |
| 6~9차 | 밸런스·포획·쓰러짐·교체 / 맵 4지역+아이템 / 이펙트 강화 |
| 10~13차 | 퀴즈 27종(한글+숫자) / 낱말 990개 / 커리큘럼 단계 |
| 14~16차 | 포켓몬 322마리 / 특별폼(알로라·히스이·거다이·메가) / **로켓단 배틀(로사·로이·나옹)** |
| 17차 | **손가락 글씨쓰기(획순)** — 커밋 `11f2461` |
| 18차 | **어른 난이도 설정 + 수학 13종 + 시계 개선** — 커밋 `01907d1` |

## 아빠가 최근에 한 말 (원문 — 중요)
- 획순에 대해: **"저건 내가 평상시에 싸울떄나 기술쓸대 나오지 않고 별도 화면이네 / 그럼 애가 저거하는거에 메리트를 못느끼지않을까?"**
- **"이게 화면이 조금은 작아져도 최대한키우고 다른걸 줄이더라도 글자를 쓰는거로 연습시키는게 현실감이 있을것같아 ㅈ만 쓰거나 이런거보다는"**
  → 낱자(ㅈ) 말고 **완성된 글자(음절)** 위주로, 캔버스는 **최대한 크게**
- 난이도: "지금 좀 쉽다고 느껴지긴해"
- 하준이가 원한 것: "로켓단이 나와서 싸우는게 있었으면 좋겟대" (완료)

## 다음에 할 일 → `spec-write2.md` 를 보세요
19차 = **획순 쓰기를 배틀에 녹이기 + 캔버스 확대 + 음절 중심**

## 그 뒤 백로그
1. `spec-music.md` — 배경음악
2. 연타 전투 (버튼 연타로 기술 강화)
3. `spec-newmons3.md` 뒷부분 — 포켓몬 4차 추가 후보
4. 받침 있는 글자 획순 검출 강화 (지금 97.1%, 받침칸이 작아서 약함. 2단계 올라가면 필요)

## 알려진 자잘한 문제 (아직 안 고침)
- 배틀 버튼이 4개라 360px 폭에서 각 77px 로 좁음
- 필살기 이펙트가 상단바를 살짝 덮음
- 얕은물/깊은물 색 구분이 약함
- "🔥 산꼭대기" 가 360px 에서 줄바꿈
- `N_SUB` 가 답 0 을 만들 수 있음
- 어른 설정(⚙️) 화면이 **작은 폰에서 세로 넘칠 가능성 — 미검증**
- 획순 쓰기 **실제 손가락 입력 미검증** (합성 터치로만 테스트). 특히 **아이폰 Safari**

## 검증 방법 (매번 이렇게 하세요)
```bash
# 1. JS 문법
cd "/d/하준/HAJUN"
SP="<scratchpad>"
sed -n '/<script>/,/<\/script>/p' game.html | sed '/<script>/d;/<\/script>/d' > "$SP/chk.js"
node --check "$SP/chk.js"
```

**node 에서 게임 로직을 직접 돌려볼 수 있습니다.** 스크립트가 IIFE 로 감싸져 있으니
테스트 코드를 **마지막 `})();` 앞에 끼워 넣어야** 내부 변수(`QUIZ_TYPES` 등)가 보입니다.
DOM 껍데기 하네스는 `scratchpad/harness.js` 방식 참고 (Proxy 로 document/window 스텁).

브라우저는 **한글 경로를 못 엽니다.** `game.html` 을 scratchpad 에 ASCII 이름으로 복사해서 여세요.

## 배포 (메인 에이전트만)
```bash
cd "/d/하준/HAJUN" && node build.js && git add -A && git commit -m "..." && git push origin main
```

## 절대 지킬 것
- **6살이 울면 안 된다.** 벌 금지, 갇힘 금지, 되돌릴 수 없는 손실 금지
- **한국어가 자연스러워야 한다.** 한글 학습 게임이라 어색한 한국어는 해롭다
  - 조사 정확히 (`josa()` / 으로·로는 `josaRo()` — 받침 ㄹ 예외)
  - 수사 정확히 ("삼 개" ✗ → "세 개" ✓)
  - 일본어 직역 금지. 로켓단은 **로사·로이·나옹** (무사시·코지로 ✗)
- 폰 우선. 360×640 / 375×812 / 384×832 / 320×568 에서 넘침 0, 터치 44px 이상
- `SAVE_KEY = 'pokemon-catch-v2'`
- 1~18차 기능 훼손 금지
